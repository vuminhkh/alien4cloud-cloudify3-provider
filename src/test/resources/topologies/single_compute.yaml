node_templates:
  compute:
    type: tosca.nodes.Compute
    properties:
      os_type: linux
      os_distribution: ubuntu
      os_version: ubuntu

dependencies:
  - name: tosca-normative-types
    version: 1.0.0.wd03-SNAPSHOT