#parse("provider/common/generic_resource.yaml.vm")
#foreach($network in ${deployment.internalNetworks})
#providerResourceNode(${network.nodeTemplate.type}, $network)
  ${network.id}_subnet:
    type: cloudify.openstack.nodes.Subnet
    properties:
      resource_id: ${network.id}_subnet
      subnet:
        ip_version: ${network.nodeTemplate.properties.get("ip_version").value}
        cidr: ${network.nodeTemplate.properties.get("cidr").value}
    relationships:
      - target: ${network.id}
        type: cloudify.relationships.contained_in
#end