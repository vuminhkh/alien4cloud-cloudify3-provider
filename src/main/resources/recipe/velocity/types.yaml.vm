## Compute types
#if($util.collectionHasElement(${deployment.nativeTypes}))
#foreach($nativeType in ${deployment.nativeTypes})
  ${nativeType.elementId}:
    derived_from: ${util.nativeType.mapToCloudifyType($nativeType)}
    properties:
      ${mapping.nativeTypePropertyName}:
        default: ${util.nativeType.getCloudifyNativeType($nativeType)}
#end
#end